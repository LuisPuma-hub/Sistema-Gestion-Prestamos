<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SistemaPrestamos.Mobile.Views.PerfilPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title=""
    BackgroundColor="#F8F7FC"
    Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Spacing="0">
            <Border
                BackgroundColor="#1D4ED8"
                StrokeThickness="0"
                StrokeShape="RoundRectangle 0,0,28,28"
                Padding="16,48,16,32">
                <Grid ColumnDefinitions="44,*,44" VerticalOptions="Center">
                    <Grid
                        Grid.Column="0"
                        HeightRequest="44"
                        WidthRequest="44"
                        HorizontalOptions="Start"
                        VerticalOptions="Center">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnVolverClicked" />
                        </Grid.GestureRecognizers>
                        <Image
                            Source="ic_chev_l_w.png"
                            WidthRequest="24"
                            HeightRequest="24"
                            HorizontalOptions="Center"
                            VerticalOptions="Center" />
                    </Grid>
                    <Label
                        Grid.Column="1"
                        Text="Mi perfil"
                        FontSize="20"
                        FontAttributes="Bold"
                        TextColor="White"
                        HorizontalTextAlignment="Center"
                        VerticalOptions="Center" />
                    <BoxView
                        Grid.Column="2"
                        WidthRequest="44"
                        BackgroundColor="Transparent" />
                </Grid>
            </Border>

            <VerticalStackLayout Padding="16,16,16,16" Spacing="14">

                <Border Padding="20,18" BackgroundColor="White" StrokeThickness="0" StrokeShape="RoundRectangle 16">
                    <VerticalStackLayout Spacing="4">
                        <Border
                            HeightRequest="72" WidthRequest="72"
                            HorizontalOptions="Center"
                            BackgroundColor="#DBEAFE"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 36">
                            <Label
                                x:Name="AvatarLabel"
                                Text="C"
                                FontSize="30"
                                FontAttributes="Bold"
                                TextColor="#1D4ED8"
                                HorizontalOptions="Center"
                                VerticalOptions="Center" />
                        </Border>
                        <Label
                            x:Name="NombreLabel"
                            FontSize="20"
                            FontAttributes="Bold"
                            TextColor="#111827"
                            HorizontalTextAlignment="Center" />
                        <Label
                            x:Name="EmailLabel"
                            FontSize="14"
                            TextColor="#6B7280"
                            HorizontalTextAlignment="Center" />
                        <Border
                            Padding="12,4"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 10"
                            HorizontalOptions="Center"
                            BackgroundColor="#DBEAFE">
                            <Label
                                x:Name="RolLabel"
                                Text=""
                                FontSize="12"
                                FontAttributes="Bold"
                                TextColor="#1D4ED8"
                                HorizontalTextAlignment="Center" />
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <Border Padding="6,4" BackgroundColor="White" StrokeThickness="0" StrokeShape="RoundRectangle 16">
                    <VerticalStackLayout Spacing="0">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" Padding="12,12">
                        <Image Grid.Column="0" Source="ic_mail.png" WidthRequest="20" HeightRequest="20" HorizontalOptions="Center" VerticalOptions="Center" />
                            <VerticalStackLayout Grid.Column="1" Spacing="0">
                                <Label Text="Correo" FontSize="12" TextColor="#9CA3AF" />
                                <Label x:Name="EmailDetalleLabel" Text="" FontSize="15" FontAttributes="Bold" TextColor="#111827" />
                            </VerticalStackLayout>
                        </Grid>
                        <BoxView HeightRequest="1" Color="#F3F4F6" Margin="12,0" />
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" Padding="12,12">
                            <Image Grid.Column="0" Source="ic_shield.png" WidthRequest="20" HeightRequest="20" HorizontalOptions="Center" VerticalOptions="Center" />
                            <VerticalStackLayout Grid.Column="1" Spacing="0">
                                <Label Text="Rol" FontSize="12" TextColor="#9CA3AF" />
                                <Label x:Name="RolDetalleLabel" Text="" FontSize="15" FontAttributes="Bold" TextColor="#111827" />
                            </VerticalStackLayout>
                        </Grid>
                        <BoxView HeightRequest="1" Color="#F3F4F6" Margin="12,0" />
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" Padding="12,12">
                            <Image Grid.Column="0" Source="ic_info.png" WidthRequest="20" HeightRequest="20" HorizontalOptions="Center" VerticalOptions="Center" />
                            <VerticalStackLayout Grid.Column="1" Spacing="0">
                                <Label Text="Versión app" FontSize="12" TextColor="#9CA3AF" />
                                <Label x:Name="VersionLabel" Text="Sistema de Préstamos v1.0" FontSize="15" FontAttributes="Bold" TextColor="#111827" />
                            </VerticalStackLayout>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <Border Padding="6,4" BackgroundColor="White" StrokeThickness="0" StrokeShape="RoundRectangle 16">
                    <Grid
                        x:Name="BiometriaGrid"
                        ColumnDefinitions="Auto,*,Auto"
                        ColumnSpacing="12"
                        Padding="12,12"
                        IsVisible="False">
                        <Image Grid.Column="0" Source="ic_lock.png" WidthRequest="20" HeightRequest="20" HorizontalOptions="Center" VerticalOptions="Center" />
                        <VerticalStackLayout Grid.Column="1" Spacing="0" VerticalOptions="Center">
                            <Label Text="Desbloqueo con huella" FontSize="15" FontAttributes="Bold" TextColor="#111827" />
                            <Label Text="Biometría disponible" FontSize="12" TextColor="#9CA3AF" />
                        </VerticalStackLayout>
                        <Switch
                            Grid.Column="2"
                            x:Name="BiometriaSwitch"
                            OnColor="#1D4ED8"
                            VerticalOptions="Center"
                            Toggled="OnBiometriaToggled" />
                    </Grid>
                </Border>

                <Button
                    x:Name="CambiarClaveButton"
                    Text="Cambiar contraseña"
                    BackgroundColor="White"
                    TextColor="#1D4ED8"
                    FontAttributes="Bold"
                    BorderColor="#1D4ED8"
                    BorderWidth="1.5"
                    CornerRadius="12"
                    HeightRequest="52"
                    Clicked="OnCambiarClaveClicked" />

                <Button
                    x:Name="CerrarSesionButton"
                    Text="Cerrar sesión"
                    BackgroundColor="#FEE2E2"
                    TextColor="#DC2626"
                    FontAttributes="Bold"
                    CornerRadius="12"
                    HeightRequest="52"
                    Clicked="OnCerrarSesionClicked" />

                <ActivityIndicator
                    x:Name="CargandoIndicator"
                    IsVisible="False"
                    IsRunning="False"
                    Color="#1D4ED8"
                    HorizontalOptions="Center" />

                <Label
                    x:Name="ErrorLabel"
                    TextColor="#DC2626"
                    IsVisible="False"
                    HorizontalTextAlignment="Center" />

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
