<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SistemaPrestamos.Mobile.Views.ArranquePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title=""
    BackgroundColor="#15307A">

    <VerticalStackLayout
        Padding="32"
        Spacing="12"
        VerticalOptions="Center">

        <Border
            HeightRequest="96"
            WidthRequest="96"
            HorizontalOptions="Center"
            BackgroundColor="White"
            StrokeThickness="0"
            StrokeShape="RoundRectangle 24">
            <Label
                Text="P"
                FontSize="52"
                FontAttributes="Bold"
                TextColor="#15307A"
                HorizontalOptions="Center"
                VerticalOptions="Center" />
        </Border>

        <Label
                    Text="CrediVzla"
            FontSize="30"
            FontAttributes="Bold"
            TextColor="White"
            HorizontalTextAlignment="Center" />

        <Label
            Text="SISTEMA DE PRÉSTAMOS"
            FontSize="12"
            TextColor="#BFDBFE"
            CharacterSpacing="2"
            HorizontalTextAlignment="Center" />

        <ActivityIndicator
            IsVisible="True"
            IsRunning="True"
            Color="White"
            HeightRequest="48"
            HorizontalOptions="Center"
            Margin="0,32,0,0" />

    </VerticalStackLayout>

</ContentPage>
